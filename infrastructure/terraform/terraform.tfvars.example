# terraform.tfvars is always gitignored
# AWS credentials are NOT set here, they come from the AWS CLI / env vars.

region             = "eu-central-1"
cluster_name       = "portfolio-eks"
kubernetes_version = "1.31"
node_instance_type = "t3.medium"
node_desired_size  = 2