apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-prod
  namespace: default
  labels:
    app: portfolio
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio
      env: prod
  template:
    metadata:
      labels:
        app: portfolio
        env: prod
    spec:
      containers:
      - name: portfolio
        image: petarpdev/devops-portfolio:prod-e0368a33adbcbc2c17f31419b51d5ce079c96f4d
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"